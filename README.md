
  # API Testing Interface Module (Copy)

  This is a code bundle for API Testing Interface Module (Copy). The original project is available at https://www.figma.com/design/u6EeHCiThUNS3CnjhE0gSM/API-Testing-Interface-Module--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  